<%= simple_form_for @plant  do |f| %>

    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :care_instructions %>
    <%= f.input :price %>
    <%= f.button :submit, class: "btn btn-ghost" %>

<%end%>
